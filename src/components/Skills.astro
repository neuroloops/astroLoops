---
import * as section from '@/content/sections/skills.md'

const { title, name, list } = section.frontmatter

console.log(list)
---

<div class="grid gap-2 md:grid-cols-2">
	{
		list.map((skill) => {
			return <div class="capitalize">{skill.name}</div>
		})
	}
</div>
