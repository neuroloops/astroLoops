---
import { twMerge } from 'tailwind-merge'

const { class: className } = Astro.props
---

<div class={twMerge('flex justify-center bg-white', className)}>
	<section class="w-5/6 max-w-6xl">
		<slot />
	</section>
</div>
